Both versions: C# 1.26.0 and Electron 1.15.0.

- **Resizing a selection follows the mouse.** It used to break off as soon as the mouse left the line by a couple of pixels.
- **A stroke continues when it comes back onto the canvas** instead of stopping at the edge.
- **Right triangle among the shapes**, next to the ordinary one.
- A gesture whose release was never seen (the button let go outside the window) no longer hangs.

---

Обе версии: C# 1.26.0, Electron 1.15.0.

- **Ресайз выделения идёт за мышью**, а не срывается при отклонении на пару пикселей.
- **Мазок продолжается, когда мышь возвращается на холст**, а не обрывается на краю.
- **Прямоугольный треугольник в фигурах**, рядом с обычным.
- Жест, отпускания которого не было видно (кнопку отпустили за окном), больше не виснет.
